@{
    var db = new PiF.Models.PiFDbDataContext().Games;
    var term = Request["term"] + "%";
    var result = db.Where(oh => oh.Name.Contains(term));
    var data = result.Select(p => new{label = p.Name});
    Json.Write(data, Response.Output);
}