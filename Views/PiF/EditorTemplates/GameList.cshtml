@using System.Collections
@using Telerik.Web.Mvc.UI
@using PiF.Models
@model Game
@*@Html.Telerik().DropDownList().Name("ID").BindTo(new SelectList((IEnumerable)this.ViewData["games"], "ID", "Name"))*@
@(Html.Telerik().AutoComplete().Name("Name")
            .Encode(false)
            .DataBinding(dataBinding => dataBinding.Ajax().Select("_AutoCompleteAjaxLoading", "PiF"))
            .AutoFill(true)
            .HtmlAttributes(new { style = "width:350px" })
            .HighlightFirstMatch(true)
            .Filterable(filtering => filtering.FilterMode(AutoCompleteFilterMode.StartsWith)))
