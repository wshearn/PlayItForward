@if (this.Request.IsAuthenticated)
{
    <text>Welcome <strong>@this.User.Identity.Name</strong>!
        [ @this.Html.ActionLink("Sign out", "LogOff", "Account") ]</text>
}
else
{
    @:[ @this.Html.ActionLink("Sign in", "Login", "Account") ]
}