function GetSelfText(thingId) {
    $.getJSON("http://www.reddit.com/r/playitforward/comments/" + thingId + "/.json?jsonp=?", { id: thingId }, function(data) {
        var tempHtml = $('<div/>').html(data[0].data.children[0].data.selftext_html).text();
        $("#selfText-" + thingId).html(tempHtml);
    }).success(function() {
        $('div.md').expander({ slicePoint: 250 });
    });
}
$(document).ready(function () { $('div.md').expander({ slicePoint: 250 }); jQuery("time.timeago").timeago() }); (function ($) {
    $.fn.styleddropdown = function () {
        return this.each(function () {
            var obj = $(this); obj.find(".field").click(function () {
                obj.find(".list").fadeIn(400);
                $(document).keyup(function (event) { if (event.keyCode == 27) { obj.find(".list").fadeOut(400) } }); obj.find(".list").hover(function () { }, function () { $(this).fadeOut(400) })
            }); obj.find(".list li").click(function () { obj.find(".field").val($(this).html()).css({ background: "#fff", color: "#333" }); obj.find(".list").fadeOut(400) })
        })
    }
})(jQuery); $(function () { $(".size").styleddropdown() });